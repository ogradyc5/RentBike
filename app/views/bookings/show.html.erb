<!DOCTYPE html>
<html lang='en'>

<h3>Booking Confirmation</h3>
<p>Your booking is confirmed for
  <%= @store_bike.name %>:
  <%= @booking.start_time.strftime('%e %b %Y %H:%M%p') %>
  until
  <%= (@booking.calculate_end_time + 60).strftime('%e %b %Y %H:%M%p') %>
  <% @bookings.each do |booking| %>
    <%= booking.length * @store_bike.bike.price %>
  <% end %>
  
  <ul class="listing">
    <div class="row center">
      <div class="col-md-4 col-md-offset-4">
        <li>Booked by:</li>
          <li><%= link_to gravatar_for(@booking.user_id), user_path(@booking.user_id) %></li>
            <li class="booking-title">
              <%= link_to @booking.user_id.email, user_path(@booking.user_id) %>
            </li>
            <li><small>
              <%= pluralize(@booking.user_id.bookings.count, "booking") if @booking.user_id.bookings %>
            </small></li>
          </li>
        </li>
      </div>
    </div>
  </ul>
  
  
  
  
  
  
</html>