<h1 align="center"> <%= link_to gravatar_for(current_user, size: 150) %> Welcome <%= current_user.email %></h3>
  <h3 align="center">Your Booking History</h3>
    <div class="row">
      <div class="col-md-4 col-md-offset-4 center">


<!--%= @bookings = Booking.where("user_id = ?", current_user.id) %>
<!--%= render 'bookings/booking', obj: @bookings %>